{
  "name": "pi-desktop-plugin-architecture-visualization",
  "version": "0.1.0",
  "private": true,
  "description": "PI-Desktop plugin that builds evidence-backed architecture models from a repository and derives diagrams, reports and health checks from them.",
  "type": "commonjs",
  "main": "main.js",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18.17.0"
  }
}
